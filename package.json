{
  "name": "subscription-watcher-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "node dist/index.js",
    "dev": "npx tsx server/index.ts"
  },
  "dependencies": {
    "express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.20.6",
    "typescript": "~5.9.2"
  }
}
